ValidatorDefinition = function (constructor) {
	this._options = {};
	this._validate 

	constructor.call(this);
};

ValidatorDefinition.prototype.validate = function (validate) {

};

Validator = function (constructor) {
	var definition = new ValidatorDefinition(constructor);

	var Validator = function (validatorOptions) {

	};

	return Validator;
};

var StringValidator = Validator(function () {
	this.validate(function (value) {

	});
});
